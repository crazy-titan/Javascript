/* if , if else , else , switch ( if any case matched than it will print 
    whole console below it except default, so;we have to use break)
    */

    // every functions executed on the basis of truthness

    // falsly elements ( "",null,undefined, NaN,false,0,-0, BigInt = 0n)

    // truly elements (" ", "0",[],{},function(){})
    // terniary =>  condition ? true:false      // VVI
    // ??   // in case of database

    // just take it easy man